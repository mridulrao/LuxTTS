services:
  luxtts-ws:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: luxtts-ws
    ports:
      - "8765:8765"
    volumes:
      - ./zipvoice/voices:/voices
      - ./.cache/huggingface:/models/huggingface
      - ./.cache/torch:/models/torch
    command:
      - python
      - -m
      - zipvoice.ws_tts_server
      - --host
      - 0.0.0.0
      - --port
      - "8765"
      - --device
      - cpu
      - --reference-dir
      - /voices
      - --prompt-audio
      - voice_sample_female.mp3
      - --prewarm
    restart: unless-stopped
